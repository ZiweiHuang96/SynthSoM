clear
clc
%%
load time1-10.mat

% The CIR information stored in the tdms file is read
len = length(ConvertedData.Data.MeasuredData(3).Data);
CIR = cell(1,len);
for j = 1:length(CIR)
    CIR{j} = ConvertedData.Data.MeasuredData(3).Data(j) + ConvertedData.Data.MeasuredData(4).Data(j) * i;
end
save time1-10_CIR.mat CIR
%%
load time11-20.mat

% The CIR information stored in the tdms file is read
len = length(ConvertedData.Data.MeasuredData(3).Data);
CIR = cell(1,len);
for j = 1:length(CIR)
    CIR{j} = ConvertedData.Data.MeasuredData(3).Data(j) + ConvertedData.Data.MeasuredData(4).Data(j) * i;
end
save time11-20_CIR.mat CIR
%%
load time21-30.mat

% The CIR information stored in the tdms file is read
len = length(ConvertedData.Data.MeasuredData(3).Data);
CIR = cell(1,len);
for j = 1:length(CIR)
    CIR{j} = ConvertedData.Data.MeasuredData(3).Data(j) + ConvertedData.Data.MeasuredData(4).Data(j) * i;
end
save time21-30_CIR.mat CIR
%%
load time31-40.mat
% The CIR information stored in the tdms file is read
len = length(ConvertedData.Data.MeasuredData(3).Data);
CIR = cell(1,len);
for j = 1:length(CIR)
    CIR{j} = ConvertedData.Data.MeasuredData(3).Data(j) + ConvertedData.Data.MeasuredData(4).Data(j) * i;
end
save time31-40_CIR.mat CIR
%%
load time41-50.mat

% The CIR information stored in the tdms file is read
len = length(ConvertedData.Data.MeasuredData(3).Data);
CIR = cell(1,len);
for j = 1:length(CIR)
    CIR{j} = ConvertedData.Data.MeasuredData(3).Data(j) + ConvertedData.Data.MeasuredData(4).Data(j) * i;
end
save time41-50_CIR.mat CIR
%%
load time51-60.mat

% The CIR information stored in the tdms file is read
len = length(ConvertedData.Data.MeasuredData(3).Data);
CIR = cell(1,len);
for j = 1:length(CIR)
    CIR{j} = ConvertedData.Data.MeasuredData(3).Data(j) + ConvertedData.Data.MeasuredData(4).Data(j) * i;
end
save time51-60_CIR.mat CIR
%%
load time61-70.mat

% The CIR information stored in the tdms file is read
len = length(ConvertedData.Data.MeasuredData(3).Data);
CIR = cell(1,len);
for j = 1:length(CIR)
    CIR{j} = ConvertedData.Data.MeasuredData(3).Data(j) + ConvertedData.Data.MeasuredData(4).Data(j) * i;
end
save time61-70_CIR.mat CIR
%%
load time71-80.mat

% The CIR information stored in the tdms file is read
len = length(ConvertedData.Data.MeasuredData(3).Data);
CIR = cell(1,len);
for j = 1:length(CIR)
    CIR{j} = ConvertedData.Data.MeasuredData(3).Data(j) + ConvertedData.Data.MeasuredData(4).Data(j) * i;
end
save time71-80_CIR.mat CIR
%%
load time81-90.mat

% The CIR information stored in the tdms file is read
len = length(ConvertedData.Data.MeasuredData(3).Data);
CIR = cell(1,len);
for j = 1:length(CIR)
    CIR{j} = ConvertedData.Data.MeasuredData(3).Data(j) + ConvertedData.Data.MeasuredData(4).Data(j) * i;
end
save time81-90_CIR.mat CIR
%%
load time91-139.mat

% The CIR information stored in the tdms file is read
len = length(ConvertedData.Data.MeasuredData(3).Data);
CIR = cell(1,len);
for j = 1:length(CIR)
    CIR{j} = ConvertedData.Data.MeasuredData(3).Data(j) + ConvertedData.Data.MeasuredData(4).Data(j) * i;
end
save time91-139_CIR.mat CIR
%%
load time140-188.mat

% The CIR information stored in the tdms file is read
len = length(ConvertedData.Data.MeasuredData(3).Data);
CIR = cell(1,len);
for j = 1:length(CIR)
    CIR{j} = ConvertedData.Data.MeasuredData(3).Data(j) + ConvertedData.Data.MeasuredData(4).Data(j) * i;
end
save time140-188_CIR.mat CIR

%%
load time189-237.mat

% The CIR information stored in the tdms file is read
len = length(ConvertedData.Data.MeasuredData(3).Data);
CIR = cell(1,len);
for j = 1:length(CIR)
    CIR{j} = ConvertedData.Data.MeasuredData(3).Data(j) + ConvertedData.Data.MeasuredData(4).Data(j) * i;
end
save time189-237_CIR.mat CIR

%%
load time238-287.mat

% The CIR information stored in the tdms file is read
len = length(ConvertedData.Data.MeasuredData(3).Data);
CIR = cell(1,len);
for j = 1:length(CIR)
    CIR{j} = ConvertedData.Data.MeasuredData(3).Data(j) + ConvertedData.Data.MeasuredData(4).Data(j) * i;
end
save time238-287_CIR.mat CIR

%%
load time288-337.mat

% The CIR information stored in the tdms file is read
len = length(ConvertedData.Data.MeasuredData(3).Data);
CIR = cell(1,len);
for j = 1:length(CIR)
    CIR{j} = ConvertedData.Data.MeasuredData(3).Data(j) + ConvertedData.Data.MeasuredData(4).Data(j) * i;
end
save time288-337_CIR.mat CIR


%%
load time338-387.mat

% The CIR information stored in the tdms file is read
len = length(ConvertedData.Data.MeasuredData(3).Data);
CIR = cell(1,len);
for j = 1:length(CIR)
    CIR{j} = ConvertedData.Data.MeasuredData(3).Data(j) + ConvertedData.Data.MeasuredData(4).Data(j) * i;
end
save time338-387_CIR.mat CIR

%%
load time388-882.mat

% The CIR information stored in the tdms file is read
len = length(ConvertedData.Data.MeasuredData(3).Data);
CIR = cell(1,len);
for j = 1:length(CIR)
    CIR{j} = ConvertedData.Data.MeasuredData(3).Data(j) + ConvertedData.Data.MeasuredData(4).Data(j) * i;
end
save time388-882_CIR.mat CIR
